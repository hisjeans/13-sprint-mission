plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.14'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.sprint.mission'
version = '1.2-M8' // 미션 8, 버전 변경

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    // Spring Data JPA를 위한 의존성 추가
    // JPA를 프로젝트에 들이고 인터페이스만 선언하면 구현체를 자동 생성하도록 한다
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    // Actuator - 애플리케이션 모니터링(상태, 정보, 지표)
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    // 유효성 검증할 수 있는 라이브러리 추가
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    // PostgreSQL을 위한 의존성 추가
    // 개발 및 운영 DB = PostgreSQL 드라이버 (실행할 때만 필요하기 때문에 runtimeOnly)
    runtimeOnly 'org.postgresql:postgresql'
    // 테스트 전용 DB= H2 인메모리 (테스트는 진짜 PostgreSQL 없이 빠르게)
    runtimeOnly 'com.h2database:h2'
    testCompileOnly 'org.projectlombok:lombok'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    testAnnotationProcessor 'org.projectlombok:lombok'
    implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.17'
    // SpringDoc 자동 스캔

    // AWS SDK for Java v2 (S3Client로 업로드/삭제, S3Presigner 로 presigned URL)
    implementation platform('software.amazon.awssdk:bom:2.46.7')
    implementation 'software.amazon.awssdk:s3'

}

tasks.named('test') {
    useJUnitPlatform()
}
